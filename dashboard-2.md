'use client'

import { useMemo, useState } from 'react'
import { Area, AreaChart, CartesianGrid, Cell, Pie, PieChart, XAxis, YAxis } from 'recharts'
import { ArrowDownLeft, ArrowUpRight, Bell, BookOpen, ChevronDown, CircleHelp, CreditCard, Download, FileText, Home, Landmark, LayoutGrid, Menu, MoreHorizontal, Plus, Receipt, Search, Settings, ShieldCheck, UserRound, WalletCards, X } from 'lucide-react'
import { ChartContainer, ChartTooltip, ChartTooltipContent } from '@/components/ui/chart'

const payments = [
  { student: 'Emma Dubois', className: 'CM2 A', amount: '145,00 €', mode: 'Carte', date: "Aujourd’hui, 09:42", status: 'Payé', initials: 'ED', color: 'bg-brand-soft text-brand' },
  { student: 'Léo Martin', className: 'CP B', amount: '82,50 €', mode: 'Espèces', date: "Aujourd’hui, 09:18", status: 'Payé', initials: 'LM', color: 'bg-amber-soft text-amber-strong' },
  { student: 'Nina Bernard', className: 'CE1 A', amount: '210,00 €', mode: 'Virement', date: 'Hier, 16:34', status: 'En attente', initials: 'NB', color: 'bg-violet-soft text-violet' },
  { student: 'Hugo Petit', className: 'CM1 B', amount: '65,00 €', mode: 'Carte', date: 'Hier, 14:12', status: 'Payé', initials: 'HP', color: 'bg-sky-soft text-sky-strong' },
  { student: 'Chloé Morel', className: 'CE2 A', amount: '120,00 €', mode: 'Carte', date: '12 sept., 11:06', status: 'Remboursé', initials: 'CM', color: 'bg-rose-soft text-rose-strong' },
]

const activities = [
  { title: 'Règlement reçu', detail: 'Emma Dubois · Cantine de septembre', time: 'Il y a 2 min', amount: '+145,00 €', icon: ArrowDownLeft, tone: 'bg-success-soft text-success' },
  { title: 'Dépense enregistrée', detail: 'Papeterie des Écoles · Fournitures', time: 'Il y a 18 min', amount: '-286,40 €', icon: ArrowUpRight, tone: 'bg-danger-soft text-danger' },
  { title: 'Règlement reçu', detail: 'Léo Martin · Étude du soir', time: 'Il y a 42 min', amount: '+82,50 €', icon: ArrowDownLeft, tone: 'bg-success-soft text-success' },
  { title: 'Virement en attente', detail: 'Nina Bernard · Transport scolaire', time: 'Hier, 16:34', amount: '210,00 €', icon: CreditCard, tone: 'bg-amber-soft text-amber-strong' },
]

const cashflow = [
  { month: 'Jan', encaissements: 18200, depenses: 11800 }, { month: 'Fév', encaissements: 20500, depenses: 12600 }, { month: 'Mar', encaissements: 19800, depenses: 13900 }, { month: 'Avr', encaissements: 23100, depenses: 14500 }, { month: 'Mai', encaissements: 21800, depenses: 15100 }, { month: 'Juin', encaissements: 24600, depenses: 15800 }, { month: 'Juil', encaissements: 22800, depenses: 13200 }, { month: 'Août', encaissements: 27100, depenses: 16400 }, { month: 'Sept', encaissements: 28900, depenses: 17100 },
]
const paymentMix = [{ mode: 'Carte', value: 48 }, { mode: 'Virement', value: 31 }, { mode: 'Espèces', value: 14 }, { mode: 'Chèque', value: 7 }]
const cashflowConfig = { encaissements: { label: 'Encaissements', color: 'var(--chart-1)' }, depenses: { label: 'Dépenses', color: 'var(--chart-2)' } }
const mixConfig = { value: { label: 'Part', color: 'var(--chart-3)' } }

function BrandMark() { return <div className="flex size-9 items-center justify-center rounded-xl bg-brand text-brand-foreground shadow-sm"><ShieldCheck className="size-5" strokeWidth={2.5} /></div> }
function Sidebar({ open, onClose }: { open: boolean; onClose: () => void }) {
  const [active, setActive] = useState('Vue d’ensemble')
  const items = [{ label: 'Vue d’ensemble', icon: Home }, { label: 'Règlements', icon: WalletCards, count: '24' }, { label: 'Dépenses', icon: Receipt }, { label: 'Facturation', icon: FileText }]
  const secondary = [{ label: 'Élèves & familles', icon: UserRound }, { label: 'Rapports', icon: LayoutGrid }]
  const nav = (items: typeof secondary) => items.map(({ label, icon: Icon, count }) => <button key={label} onClick={() => setActive(label)} className={`flex items-center justify-between rounded-lg px-3 py-2.5 text-[13px] font-medium transition-colors ${active === label ? 'bg-sidebar-active text-sidebar-active-foreground shadow-sm' : 'text-sidebar-muted hover:bg-sidebar-accent hover:text-sidebar-foreground'}`}><span className="flex items-center gap-3"><Icon className="size-[17px]" />{label}</span>{count && <span className="rounded-full bg-sidebar-active-badge px-2 py-0.5 text-[10px] font-bold">{count}</span>}</button>)
  return <aside className={`fixed inset-y-0 left-0 z-30 flex w-72 flex-col border-r border-sidebar-border bg-sidebar px-4 py-5 transition-transform duration-200 lg:static lg:translate-x-0 ${open ? 'translate-x-0' : '-translate-x-full'}`}><div className="mb-9 flex items-center justify-between px-2"><div className="flex items-center gap-3"><BrandMark /><div><p className="font-display text-[17px] font-bold tracking-tight text-sidebar-foreground">payskool</p><p className="text-[10px] font-semibold uppercase tracking-[0.18em] text-sidebar-muted">Gestion scolaire</p></div></div><button onClick={onClose} className="rounded-lg p-2 text-sidebar-muted hover:bg-sidebar-accent lg:hidden" aria-label="Fermer le menu"><X className="size-5" /></button></div><button className="mb-7 flex w-full items-center justify-between rounded-xl border border-sidebar-border bg-sidebar-card px-3 py-3 text-left hover:bg-sidebar-accent"><span className="flex items-center gap-3"><span className="flex size-9 items-center justify-center rounded-lg bg-sidebar-brand text-sidebar-brand-foreground"><Landmark className="size-4" /></span><span><span className="block text-[13px] font-semibold text-sidebar-foreground">École Saint-Exupéry</span><span className="mt-0.5 block text-[11px] text-sidebar-muted">2024 — 2025</span></span></span><ChevronDown className="size-4 text-sidebar-muted" /></button><p className="mb-2 px-3 text-[10px] font-bold uppercase tracking-[0.16em] text-sidebar-muted">Navigation</p><nav className="flex flex-col gap-1">{nav(items)}</nav><p className="mb-2 mt-8 px-3 text-[10px] font-bold uppercase tracking-[0.16em] text-sidebar-muted">Organisation</p><nav className="flex flex-col gap-1">{nav(secondary)}</nav><div className="mt-auto flex flex-col gap-1 border-t border-sidebar-border pt-4"><button className="flex items-center gap-3 rounded-lg px-3 py-2.5 text-[13px] font-medium text-sidebar-muted hover:bg-sidebar-accent"><CircleHelp className="size-[17px]" />Centre d’aide</button><button className="flex items-center gap-3 rounded-lg px-3 py-2.5 text-[13px] font-medium text-sidebar-muted hover:bg-sidebar-accent"><Settings className="size-[17px]" />Paramètres</button><div className="mt-3 flex items-center gap-3 border-t border-sidebar-border px-3 pt-4"><div className="flex size-9 items-center justify-center rounded-full bg-sidebar-avatar text-xs font-bold text-sidebar-avatar-foreground">MS</div><div className="min-w-0 flex-1"><p className="truncate text-[12px] font-semibold text-sidebar-foreground">Marie Sauvage</p><p className="truncate text-[11px] text-sidebar-muted">Administratrice</p></div><MoreHorizontal className="size-4 text-sidebar-muted" /></div></div></aside>
}
function MetricCard({ label, value, change, detail, icon: Icon, accent }: { label: string; value: string; change: string; detail: string; icon: typeof WalletCards; accent: string }) { return <div className="rounded-2xl border border-border bg-card p-5 shadow-card"><div className="flex items-start justify-between"><div className={`flex size-10 items-center justify-center rounded-xl ${accent}`}><Icon className="size-[18px]" /></div><span className="rounded-full bg-success-soft px-2.5 py-1 text-[11px] font-bold text-success">{change}</span></div><p className="mt-5 text-[12px] font-medium text-muted-foreground">{label}</p><p className="mt-1 font-display text-[25px] font-bold tracking-tight text-foreground">{value}</p><p className="mt-1 text-[11px] text-muted-foreground">{detail}</p></div> }
function Analytics({ range, setRange }: { range: string; setRange: (value: string) => void }) { return <section className="mt-7 grid gap-5 xl:grid-cols-[1.55fr_1fr]"><div className="rounded-2xl border border-border bg-card p-5 shadow-card lg:p-6"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-[11px] font-bold uppercase tracking-[0.16em] text-brand">Performance financière</p><h2 className="mt-1 font-display text-lg font-bold tracking-tight">Trésorerie de l’établissement</h2><p className="mt-1 text-xs text-muted-foreground">Une vision claire de vos flux sur l’année scolaire.</p></div><div className="flex rounded-lg border border-border bg-muted/50 p-1">{['7 jours', '30 jours', 'Année'].map((item) => <button key={item} onClick={() => setRange(item)} className={`rounded-md px-2.5 py-1.5 text-[11px] font-semibold ${range === item ? 'bg-card text-foreground shadow-sm' : 'text-muted-foreground'}`}>{item}</button>)}</div></div><div className="mt-5 flex items-end gap-3"><p className="font-display text-3xl font-bold tracking-tight">28 900 €</p><span className="mb-1 rounded-full bg-success-soft px-2 py-1 text-[10px] font-bold text-success">+12,4 %</span></div><ChartContainer config={cashflowConfig} className="mt-2 h-[230px] w-full aspect-auto"><AreaChart accessibilityLayer data={cashflow} margin={{ left: -20, right: 8, top: 12, bottom: 0 }}><defs><linearGradient id="fillIncome" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="var(--chart-1)" stopOpacity={0.22} /><stop offset="100%" stopColor="var(--chart-1)" stopOpacity={0} /></linearGradient></defs><CartesianGrid vertical={false} strokeDasharray="4 4" /><XAxis dataKey="month" tickLine={false} axisLine={false} tickMargin={10} /><YAxis tickLine={false} axisLine={false} tickMargin={8} tickFormatter={(value) => `${value / 1000}k`} /><ChartTooltip cursor={false} content={<ChartTooltipContent indicator="line" />} /><Area dataKey="encaissements" type="monotone" fill="url(#fillIncome)" stroke="var(--color-encaissements)" strokeWidth={2.5} dot={false} /><Area dataKey="depenses" type="monotone" fill="transparent" stroke="var(--color-depenses)" strokeWidth={2} strokeDasharray="5 5" dot={false} /></AreaChart></ChartContainer><div className="mt-1 flex items-center gap-5 text-[11px] text-muted-foreground"><span className="flex items-center gap-2"><span className="size-2 rounded-full bg-chart-1" />Encaissements</span><span className="flex items-center gap-2"><span className="size-2 rounded-full bg-chart-2" />Dépenses</span></div></div><div className="rounded-2xl border border-border bg-card p-5 shadow-card lg:p-6"><div className="flex items-start justify-between"><div><p className="text-[11px] font-bold uppercase tracking-[0.16em] text-brand">Répartition</p><h2 className="mt-1 font-display text-lg font-bold tracking-tight">Modes de règlement</h2><p className="mt-1 text-xs text-muted-foreground">Sur les encaissements du mois.</p></div><button className="rounded-lg p-2 text-muted-foreground hover:bg-muted" aria-label="Options du graphique"><MoreHorizontal className="size-4" /></button></div><div className="mt-6 grid grid-cols-[1fr_1.15fr] items-center gap-4"><div className="relative"><ChartContainer config={mixConfig} className="mx-auto aspect-square h-[175px]"><PieChart><Pie data={paymentMix} dataKey="value" nameKey="mode" startAngle={90} endAngle={-270} innerRadius="62%" outerRadius="88%" paddingAngle={3} cornerRadius={7} strokeWidth={0}>{paymentMix.map((item, index) => <Cell key={item.mode} fill={`var(--chart-${index + 1})`} />)}</Pie></PieChart></ChartContainer><div className="pointer-events-none absolute inset-0 flex flex-col items-center justify-center"><span className="font-display text-2xl font-bold">100%</span><span className="text-[10px] text-muted-foreground">total reçu</span></div></div><div className="flex flex-col gap-4">{paymentMix.map((item, index) => <div key={item.mode} className="flex items-center justify-between gap-3"><span className="flex items-center gap-2 text-xs font-medium"><span className={`size-2 rounded-full bg-chart-${index + 3}`} />{item.mode}</span><span className="font-display text-sm font-bold">{item.value}%</span></div>)}</div></div><div className="mt-5 rounded-xl bg-lavender-soft px-4 py-3"><p className="text-xs font-semibold text-brand">Votre taux de digitalisation est de 79 %</p><p className="mt-1 text-[11px] leading-relaxed text-muted-foreground">Les règlements en ligne progressent de 8 % ce mois-ci.</p></div></div></section> }

export default function Page() {
  const [sidebarOpen, setSidebarOpen] = useState(false); const [query, setQuery] = useState(''); const [filter, setFilter] = useState('Tous'); const [toast, setToast] = useState(''); const [range, setRange] = useState('Année')
  const filteredPayments = useMemo(() => payments.filter((payment) => (filter === 'Tous' || payment.status === filter) && `${payment.student} ${payment.className}`.toLowerCase().includes(query.toLowerCase())), [filter, query]); const showToast = (message: string) => { setToast(message); window.setTimeout(() => setToast(''), 2600) }
  return <div className="min-h-screen bg-background text-foreground"><div className="flex min-h-screen"><Sidebar open={sidebarOpen} onClose={() => setSidebarOpen(false)} /><div className="fixed inset-0 z-20 bg-foreground/20 lg:hidden" onClick={() => setSidebarOpen(false)} hidden={!sidebarOpen} aria-hidden="true" /><main className="min-w-0 flex-1"><header className="flex h-[72px] items-center justify-between border-b border-border bg-card px-5 lg:px-9"><div className="flex items-center gap-4"><button onClick={() => setSidebarOpen(true)} className="rounded-lg p-2 text-muted-foreground hover:bg-muted lg:hidden" aria-label="Ouvrir le menu"><Menu className="size-5" /></button><div className="relative hidden w-64 md:block"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-muted-foreground" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Rechercher un élève..." className="h-9 w-full rounded-lg border border-input bg-muted/50 pl-9 pr-3 text-xs outline-none placeholder:text-muted-foreground focus:border-brand focus:ring-2 focus:ring-brand/15" /></div></div><div className="flex items-center gap-3"><button onClick={() => showToast('Aucune nouvelle notification')} className="relative rounded-lg p-2 text-muted-foreground hover:bg-muted" aria-label="Notifications"><Bell className="size-[18px]" /><span className="absolute right-1.5 top-1.5 size-1.5 rounded-full bg-brand" /></button><div className="hidden h-6 w-px bg-border sm:block" /><button onClick={() => showToast('Année scolaire 2024 — 2025 sélectionnée')} className="hidden items-center gap-2 rounded-lg px-2 py-1.5 text-xs font-semibold text-muted-foreground hover:bg-muted sm:flex">Année 2024 — 2025<ChevronDown className="size-3.5" /></button><div className="flex size-8 items-center justify-center rounded-full bg-brand-soft text-[11px] font-bold text-brand">MS</div></div></header><div className="mx-auto max-w-[1450px] px-5 py-7 lg:px-9 lg:py-9"><div className="flex flex-wrap items-end justify-between gap-4"><div><div className="mb-2 flex items-center gap-2 text-xs font-medium text-muted-foreground"><Home className="size-3.5" /> / Vue d’ensemble</div><h1 className="font-display text-3xl font-bold tracking-tight lg:text-[34px]">Bonjour Marie</h1><p className="mt-1 text-sm text-muted-foreground">Voici ce qui se passe dans votre établissement aujourd’hui.</p></div><div className="flex items-center gap-2"><button onClick={() => showToast('Export préparé')} className="flex items-center gap-2 rounded-lg border border-border bg-card px-3.5 py-2.5 text-xs font-semibold shadow-sm hover:bg-muted"><Download className="size-4" />Exporter</button><button onClick={() => showToast('Nouveau règlement créé')} className="flex items-center gap-2 rounded-lg bg-brand px-3.5 py-2.5 text-xs font-bold text-brand-foreground shadow-sm hover:bg-brand-hover"><Plus className="size-4" />Nouveau règlement</button></div></div><div className="mt-7 grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><MetricCard label="Solde disponible" value="24 680,00 €" change="+12,4 %" detail="vs. mois dernier" icon={WalletCards} accent="bg-brand-soft text-brand" /><MetricCard label="Encaissements du mois" value="18 420,00 €" change="+8,2 %" detail="156 règlements reçus" icon={ArrowDownLeft} accent="bg-success-soft text-success" /><MetricCard label="Dépenses du mois" value="7 860,00 €" change="−3,1 %" detail="42 opérations" icon={ArrowUpRight} accent="bg-amber-soft text-amber-strong" /><MetricCard label="En attente" value="2 340,00 €" change="12" detail="règlements à suivre" icon={CreditCard} accent="bg-violet-soft text-violet" /></div><Analytics range={range} setRange={setRange} /><div className="mt-7 grid gap-5 xl:grid-cols-[1.6fr_1fr]"><section className="overflow-hidden rounded-2xl border border-border bg-card shadow-card"><div className="flex flex-wrap items-center justify-between gap-3 border-b border-border px-5 py-4 lg:px-6"><div><h2 className="font-display text-base font-bold">Paiements récents</h2><p className="mt-1 text-xs text-muted-foreground">Les dernières transactions enregistrées.</p></div><div className="flex items-center gap-2"><select value={filter} onChange={(event) => setFilter(event.target.value)} className="rounded-lg border border-input bg-card px-2.5 py-2 text-xs font-semibold text-muted-foreground outline-none"><option>Tous</option><option>Payé</option><option>En attente</option><option>Remboursé</option></select><button onClick={() => showToast('Tous les paiements affichés')} className="hidden rounded-lg px-2.5 py-2 text-xs font-bold text-brand hover:bg-brand-soft sm:block">Voir tout</button></div></div><div className="overflow-x-auto"><table className="w-full min-w-[650px] text-left"><thead><tr className="border-b border-border text-[10px] font-bold uppercase tracking-[0.12em] text-muted-foreground"><th className="px-5 py-3 lg:px-6">Élève</th><th className="px-3 py-3">Montant</th><th className="px-3 py-3">Mode</th><th className="px-3 py-3">Date</th><th className="px-3 py-3">Statut</th><th className="px-5 py-3 lg:px-6"></th></tr></thead><tbody className="divide-y divide-border">{filteredPayments.map((payment) => <tr key={payment.student} className="text-xs hover:bg-muted/40"><td className="px-5 py-3.5 lg:px-6"><div className="flex items-center gap-3"><div className={`flex size-8 items-center justify-center rounded-full text-[10px] font-bold ${payment.color}`}>{payment.initials}</div><div><p className="font-semibold text-foreground">{payment.student}</p><p className="mt-0.5 text-[11px] text-muted-foreground">{payment.className}</p></div></div></td><td className="px-3 py-3.5 font-display font-bold">{payment.amount}</td><td className="px-3 py-3.5 text-muted-foreground">{payment.mode}</td><td className="whitespace-nowrap px-3 py-3.5 text-muted-foreground">{payment.date}</td><td className="px-3 py-3.5"><span className={`rounded-full px-2 py-1 text-[10px] font-bold ${payment.status === 'Payé' ? 'bg-success-soft text-success' : payment.status === 'En attente' ? 'bg-amber-soft text-amber-strong' : 'bg-rose-soft text-rose-strong'}`}>{payment.status}</span></td><td className="px-5 py-3.5 text-right lg:px-6"><button onClick={() => showToast(`Reçu de ${payment.student} téléchargé`)} className="rounded-md p-1.5 text-muted-foreground hover:bg-muted" aria-label={`Télécharger le reçu de ${payment.student}`}><Download className="size-4" /></button></td></tr>)}</tbody></table></div></section><section className="rounded-2xl border border-border bg-card shadow-card"><div className="flex items-center justify-between border-b border-border px-5 py-4"><div><h2 className="font-display text-base font-bold">Activité récente</h2><p className="mt-1 text-xs text-muted-foreground">Les dernières actions de votre équipe.</p></div><button className="rounded-lg p-2 text-muted-foreground hover:bg-muted" aria-label="Options activité"><MoreHorizontal className="size-4" /></button></div><div className="flex flex-col divide-y divide-border px-5">{activities.map(({ title, detail, time, amount, icon: Icon, tone }) => <div key={detail} className="flex items-start gap-3 py-4"><div className={`mt-0.5 flex size-8 shrink-0 items-center justify-center rounded-full ${tone}`}><Icon className="size-4" /></div><div className="min-w-0 flex-1"><div className="flex items-center justify-between gap-3"><p className="text-xs font-semibold">{title}</p><span className={`whitespace-nowrap text-xs font-bold ${amount.startsWith('+') ? 'text-success' : amount.startsWith('-') ? 'text-danger' : 'text-amber-strong'}`}>{amount}</span></div><p className="mt-1 truncate text-[11px] text-muted-foreground">{detail}</p><p className="mt-1 text-[10px] text-muted-foreground">{time}</p></div></div>)}</div></section></div></div></main></div>{toast && <div role="status" className="fixed bottom-5 right-5 z-50 rounded-xl bg-foreground px-4 py-3 text-xs font-semibold text-background shadow-xl">{toast}</div>}</div>
}
